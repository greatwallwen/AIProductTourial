# 贯穿案例状态

更新时间：2026-07-24  
核查时区：Asia/Shanghai  
当前版本：`case-baseline-v0`  
当前阶段：案例方向已批准，章节实现尚未开始

## 1. 已批准案例

- 名称：电商售后工单处理助手。
- 案例章程：`codex/case/CASE_CHARTER.md`。
- 批准日期：2026-07-24。
- 当前仅批准业务方向、数据原则、跨章演进方式和权限边界。

## 2. 当前能力

无运行能力。

当前仓库尚未建立该案例的：

- 示例代码。
- 合成业务数据。
- 模型或服务配置。
- 数据库。
- 运行结果。
- 验证记录。

`case-baseline-v0` 是文档基线，不是可运行版本。

这不代表第 1 章可以只交付文档。第 1 章的完成版本必须是读者可在个人电脑上执行的 `chapter-01-v1`；在可运行程序、说明和验证证据齐备前，不得把第 1 章案例增量标记为完成。

## 3. 当前已知局限

- 尚不能读取或处理工单。
- 尚不能调用模型。
- 尚无固定输入样本和期望程序行为。
- 尚未选择语言、依赖、模型、服务或接口格式。
- 尚未核查任何具体 AI 服务在中国大陆的可用性。
- 尚未验证正常路径、错误路径、停止条件或权限边界。

## 4. 数据状态

- 当前没有案例数据。
- 已批准使用小型合成数据，并随章节需要逐步创建。
- 第 1～4 章不以数据库为前置条件。
- SQLite 仅是第 13 章以后在确有需要时可考虑的可重建载体，目前没有选用或实现。

## 5. 供应商与运行环境状态

- 当前未锁定任何 AI 供应商或模型。
- 当前未选定模型调用协议或 SDK。
- 当前没有凭据，也没有真实模型调用证据。
- 后续选型必须优先满足中国大陆无需翻墙可用、依赖可安装、凭据可安全管理和实现可替换。

## 6. 版本与复现

`case-baseline-v0` 由以下文档定义：

- `codex/case/CASE_CHARTER.md`
- `codex/case/CASE_STATUS.md`

由于尚无代码和数据，本版本没有运行命令或运行结果。

## 7. 下一步入口

当前唯一 Approved 的章节任务书是：

- `codex/briefs/chapter-01-large-language-model.BRIEF.md`

进入第 1 章案例增量时，应只完成：

1. 固定一条低风险合成工单文本。
2. 建立一次模型调用的完整可运行程序，不能只给伪代码。
3. 保存输入和未经修改的原始输出。
4. 由人触发、观察并决定是否采用输出。
5. 验证一次成功响应、缺少凭据、服务失败和调用后停止。
6. 记录环境、中国大陆可用性、命令、实际结果和未覆盖范围。
7. 提供不需要真实凭据的本地行为测试，并明确它不是模型调用。
8. 使用真实模型服务完成至少一次调用验证；没有凭据时如实记录为未验证，不用测试替身冒充。

不得提前加入数据库、历史对话、结构化输出、独立验证、工具、循环、状态或恢复能力。
