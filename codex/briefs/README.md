# 章节任务书

任务书按第二版目录中的新版章节编号命名。正式正文路径确定后，文件名与新版正文文件名保持对应，例如：

`codex/briefs/chapter-01-large-language-model.BRIEF.md`

状态依次为 `Draft for approval`、`Approved`、`Superseded`。已批准版本发生范围变化时，不静默覆盖；在文件中记录版本和变更原因。

`docs/_source/` 只作为旧版素材库，不决定新版任务书的命名、章节范围或拆分方式。新版正文路径确定前，只为当前明确授权的章节建立任务书，不批量预建空文件。
