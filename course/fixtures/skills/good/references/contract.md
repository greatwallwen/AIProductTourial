# Loop Contract Fields

- `goal`: measurable desired state.
- `maxTurns`: hard upper bound.
- `failureFingerprint`: stable identifier for repeated failure.
- `evidenceRefs`: replayable outputs used by the evaluator.
- `stopReason`: success, repeated-failure, regression, budget, or external-boundary.
