# 产品经理转型实操知识库

> 面向技术骨干与项目经理的产品经理转型实操知识库。**整体逻辑：先讲系统设计的理念、原理、工程规范与多套设计，再用不同案例来演示、验证**。真数据、可运行深色大屏原型（`code/web`）、真截图、结构化 Skill、Node 校验护栏。

> 写法：构建契约式 Prompt + 可运行成品 + 跑通纠错 + 交付物验收；可用 Trae / CodeBuddy 等任一 Agent 工具（Loop 为工具无关的开发模式）。数据真实/合成显式标注（`dataset/MANIFEST.md`）；高影响行业保留人工复核。

# 第一部分 · 系统设计理念与原理

## 1. AI 核心概念底层

![AI 核心概念底层链路](outputs/product_case_library/svg/fig_ai_foundations.svg)

> 做数字化产品，绕不开 AI。可 LLM、Token、Context、Agent 这些词，很多人只知其名、不知其义。这一章我们从底层往上，一个一个拆开——不背定义，只讲清楚它到底是怎么回事。本章工具无关，只讲原理。

### 1.1 大语言模型
#### 1.1.1 自回归
#### 1.1.2 概率预测
#### 1.1.3 结束标识

```备注
2017 年，Google 的八个人发了一篇叫《Attention Is All You Need》的论文，提出了一个叫 Transformer 的架构。当时没几个人意识到，这篇论文会把整个世界推进 AI 时代——今天你听说的几乎所有大模型，底子都是它。

那这个「大模型」到底在干嘛？说穿了，它做的事朴素得让人意外：文字接龙。你问它「朱老师的讲座怎么样？」，它不会一次想好整句答案，而是先算出「下一个最该出现的词」概率最高的是哪个——比如「特别」；然后把「特别」接到你的问题后面，当成新的输入，再算下一个词——「棒」；如此一个词一个词地往下接，直到它算出「该结束了」这个特殊信号，才收笔。

这就是为什么大模型永远是一个字一个字往外蹦，而不是唰地一下把整段甩给你——它的底层机制，本就是这样逐词生成的。理解了这一点，你就理解了它所有行为的根。
```

### 1.2 Token 与分词
#### 1.2.1 编码切分
#### 1.2.2 映射编号
#### 1.2.3 量化换算

```备注
2026 年 3 月的英伟达 GTC 大会上，黄仁勋讲了两个小时。清华的杨斌教授数了一下：整场演讲里出现频次最高的词，不是「AI」，也不是「GPU」，而是——Token，超过 70 次。就在同一天，阿里巴巴宣布成立「Token 事业部」，和淘天电商、阿里云平级。黄仁勋甚至造了个词叫「Token 经济学」。一个技术名词，怎么就成了产业的中心？

先说它是什么。大模型内部其实读不懂文字，它只认数字。所以人和模型之间，得有个翻译——Tokenizer（分词器）。它干两件事：把你输入的话切成一个个最小片段（这片段就叫 Token），再给每个 Token 绑一个数字编号（Token ID）。你问「今天天气怎么样？」，可能被切成「今天 / 天气 / 怎么样 / ？」这么几个 Token，再变成一串数字喂进模型。

这里有个最常见的误会：Token 不等于「字」。字符是固定的，1 个汉字就是 1 个字符；可 Token 不一样——有的模型把「今天」当 1 个 Token，有的拆成「今」「天」两个。所以同一句话，在不同模型里 Token 数可能不同。行业里有个粗略换算：1 个 Token≈0.75 个英文单词≈1.5~2 个汉字，40 万 Token 大约 60~80 万汉字。

那 Token 为什么这么重要？因为它是 AI 世界的「计量单位」——你花多少钱、模型能记多长、算力烧多少，全部按 Token 算。你后面会反复见到它。
```


![Tokenizer 编码：文字→Token→Token ID（真实原理图，源 AI agent 参考包）](assets/vendor/aiagent/image-20260704090742433.png)

### 1.3 上下文与窗口
#### 1.3.1 上下文
#### 1.3.2 上下文窗口
#### 1.3.3 检索增强

```备注
你有没有想过一个问题：大模型明明只是个「输入什么、输出什么」的函数，它凭什么能「记住」你上一句说了啥？

答案有点反直觉：它其实没有记忆。每次你发消息，前端程序都会悄悄把「之前的完整对话」和你这次的新问题拼在一起，一整包重新发给模型。模型每次收到的，都是完整的前情提要，所以看起来像「记得」。这一整包信息，就叫 Context（上下文）——它是模型这一次运算能看到的全部输入，是临时的记忆载体，装着历史对话、系统规则、工具数据等等。

那这个「临时记忆」能装多大？这就是 Context Window（上下文窗口）——单次能容纳的最大 Token 数。它直接决定了模型能记多长。如今主流模型的窗口已经到百万级：GPT-5.4 约 105 万 Token，Gemini 3.1 Pro、Opus 4.6 都是 100 万——按 1 Token≈1.5 汉字算，100 万 Token 差不多能装下整套《哈利·波特》。

可就算窗口再大，也有个致命问题：假如你有一份上千页的产品手册，想让 AI 依托它答用户问题，是不是每次都把整本书塞进去？答案是否定的——太贵、太吵，无关内容会干扰判断。行业的最优解叫 RAG（检索增强生成）：先用检索算法从手册里挑出和问题最相关的那几段，只把这几段 + 问题喂给模型。省钱、聚焦、还更准。这正是本教程「向量库检索」案例干的事。
```


![RAG 回答流程：Embedding→向量库→召回 10→Cross-Encoder 重排→3 片段→大模型（真实原理图，源 AI agent 参考包）](assets/vendor/aiagent/image-20260704151540085.png)

### 1.4 提示词
#### 1.4.1 用户提示
#### 1.4.2 系统提示
#### 1.4.3 提示词工程

```备注
同样一句「帮我写一首诗」，为什么有人得到惊艳的作品，有人只得到一堆打油诗？差别不在模型，在提示词（Prompt）。

Prompt 就是你给模型下的具体指令，它的精准度直接决定输出质量。你说「帮我写首诗」，模型只能瞎猜——五言、七言、古体、现代，全凭运气；可你说「帮我写一首五言绝句，主题是秋天的落叶，风格悲凉萧瑟」，它立刻就能从体裁、主题、风格三个维度精准命中。这门「把话说清楚」的功夫，就是提示词工程。

再往深一层，Prompt 其实分两种，各司其职。System Prompt（系统提示词）是开发者在后台预设的、用户看不见也改不了，负责定人设、定规则、定边界，全程生效。比如做一个数学辅导机器人，后台先固定一句：「你是耐心的小学数学老师，学生提问时禁止直接给答案，要用生活化例子一步步引导。」User Prompt（用户提示词）才是学生每轮输入的具体问题，比如「3+5 等于几？」。没有系统提示词，模型会直接甩出「8」；有了它，模型会引导：「你手里有 3 个苹果，妈妈又给你 5 个，现在一共几个？自己数数看。」——System 定规矩，User 定任务，两者配合，才有既守规则又解决问题的 AI。
```


![System Prompt 定规矩 · User Prompt 定任务（真实原理图，源 AI agent 参考包）](assets/vendor/aiagent/image-20260704093622372.png)

### 1.5 工具与协议
#### 1.5.1 工具
#### 1.5.2 四方协作
#### 1.5.3 统一接入

```备注
你问 AI：「今天上海天气怎么样？」它多半会说「抱歉，我无法获取实时数据」。为什么这么强的模型，连查个天气都不会？

因为大模型的能力全来自训练数据，本质是「文字概率预测」，它天生无法联网、无法触达真实世界。要补上这块短板，就得引入 Tool（工具）——一个可以被调用的函数：给它固定入参（比如「城市、日期」），它去调真实接口，返回结构化结果。有了工具，模型才从「纯文本脑补」变成「能对接真实世界」。

但工具不是模型自己调的，中间涉及四方：用户提问 → 平台把问题转给模型 → 模型判断「需要查天气」，生成一条标准的「工具调用指令」回传平台 → 平台真正执行工具、拿到数据、再交回模型整理成人话。模型负责思考决策，平台负责穿针引线。

问题又来了：每个 AI 平台的工具接入标准都不一样，同一个工具适配 GPT、Gemini、Claude 得写三遍代码，太浪费。于是有了 MCP（模型上下文协议）——一套通用的工具接入标准。它像手机的 Type-C 接口：统一之前各家充电口互不通用，统一之后一个充电器走天下。工具按 MCP 开发一次，就能接入所有支持它的平台。
```


![Tool 工具调用：用户/平台/大模型/工具 四方协作（真实原理图，源 AI agent 参考包）](assets/vendor/aiagent/image-20260704102308686.png)

### 1.6 智能体与技能
#### 1.6.1 智能体
#### 1.6.2 循环推理
#### 1.6.3 智能体技能

```备注
有了工具，AI 能查天气了。但真实需求往往更复杂：「帮我看看我当前位置的天气，如果下雨，就再帮我找附近的雨伞店。」这一句话，要连用定位、天气、店铺三个工具，还要根据「是否下雨」分情况——单次工具调用根本搞不定。

这就引出了 Agent（智能体）。它的本事是自主规划、分步推理、循环执行：第一步想「得先知道我在哪」，调定位工具；拿到经纬度，第二步调天气工具；判断「下雨了」，第三步才去调店铺工具找雨伞店；最后把所有结果拼成答案。这套「思考→调用工具→拿到结果→再思考」的循环，就是业内说的 ReAct 架构——它让 AI 从「被动应答」升级为「主动把复杂任务干完」。（这条循环，正是第 2 章「三层 Loop」的种子。）

可 Agent 再聪明，面对你的个性化习惯还是会抓瞎——你希望出门前它按「先看天气、再列清单、格式固定」来办，可它不知道。于是有了 Agent Skill（智能体技能）：一份固化的专属说明书，把触发条件、执行步骤、判断规则、输出格式一次写清楚，AI 一加载就照办，既复用又省 Token。本教程的 Skill 库，就是这类结构化技能。
```

![Agent 的 ReAct 完整闭环：思考→调用工具→观察→再思考（真实原理图，源 AI agent 参考包）](assets/vendor/aiagent/image-20260704154121684.png)

## 2. 理念框架（会 Loop 的产品工程）

![三层 Loop](outputs/product_case_library/svg/fig_ideology_loops.svg)

### 2.1 三层 Loop
#### 2.1.1 内层执行
#### 2.1.2 中层判断
#### 2.1.3 外层校准

```备注
AI 能自主写代码后，真正变化的不是效率，而是软件开发变成三种时间尺度的 Loop 同时运转。内层 Agentic Coding Loop：给 Spec 与 Evals，Agent 自己写码、跑测、发现问题继续改，分钟级自我纠偏。中层 Developer Feedback Loop：人不能离开，负责判断——功能范围、交互、信息组织，看完第一版再回改 Spec，几十分钟到小时级。外层 External Feedback Loop：真实用户与市场校准开发者的 Vision，几天到几周。一层比一层慢，也一层比一层重要；少任何一层，0→1 产品都难跑起来。稀缺能力正从内层向外层迁移。
```

### 2.2 提示词到 Loop
#### 2.2.1 提示词
#### 2.2.2 上下文
#### 2.2.3 脚手架
#### 2.2.4 循环

```备注
演进链路：Prompt Engineering（一条消息预见一切，靠措辞）→ Context Engineering（精心构造模型"看到"的：system prompt、few-shot、RAG、结构化输入，不再猜）→ Harness Engineering（给 Agent 装工具+重试+权限，一次 session 内多步执行）→ Loop Engineering（把"人触发→人判断→人决定下一步"里的人替换成能定时触发、验证、记忆、决定继续或停止的自动化系统）。与 cron 的区别：Loop 内部决策者是 LLM，行为在运行时才确定。
```

### 2.3 控制论闭环
#### 2.3.1 控制器
#### 2.3.2 执行器
#### 2.3.3 传感器

```备注
Loop 的本质是控制论：目标→执行→测量偏差→反馈修正→再测量。控制器（编排逻辑：读偏差、判断、构造下一轮 prompt）需要知识与记忆（→ Skills、Memory）；执行器（工具调用：改文件/命令/API）需要触达与隔离（→ Connectors、Worktrees）；传感器（验证，产出误差信号）需要独立性（→ Sub-agents）。收敛速度由传感器决定：只回 pass/fail 近乎盲猜，回"哪个用例挂、哪个断言失败、哪个 diff 引入"才是针对缺陷修复。**传感器就是设计本身**——"好提示+弱验证"会失败，"平庸提示+强验证"会收敛。
```

### 2.4 Loop 六件套
#### 2.4.1 触发
#### 2.4.2 隔离
#### 2.4.3 技能
#### 2.4.4 连接
#### 2.4.5 子代理
#### 2.4.6 记忆

```备注
六个组件是控制自然需要的基础设施：Automations（触发：定时/事件/心跳，谁按开始键）；Worktrees（隔离：每 Agent 一份独立工作区，并行不打架）；Skills（技能：把项目约定/踩坑写下来，每轮直接读，省 token）；Connectors（连接：MCP 接 GitHub/工单/通知，触达真实工具链）；Sub-agents（子代理：写的与验的分开，写代码的模型给自己打分太宽容）；Memory/State（记忆：上下文窗口每轮清空，磁盘存"做完了什么、下一步干啥"）。
```

### 2.5 何时建 Loop
#### 2.5.1 可重复
#### 2.5.2 可判定
#### 2.5.3 预算够
#### 2.5.4 可复现

```备注
Loop 不免费（烧 token、要搭、出问题要 debug 你没盯着的系统）。动手前四问：任务是否重复（一次性活好 prompt 更划算）？有没有东西能自动判定"干砸了"（测试/类型/lint/构建）？token 预算扛得住浪费？Agent 跑得了自己写的代码（有日志、能复现、看得到哪崩）？附加题最重要：你打算 review 产出吗——不打算就别建。目标不可测量的任务（"更优雅""更好"）不要建 Loop，那是假闭环。先建 Inspector，再建 Loop。
```

### 2.6 AI 软件工程框架
#### 2.6.1 强制方法论
#### 2.6.2 上下文管理
#### 2.6.3 虚拟团队
#### 2.6.4 仓库记忆

```备注
从"AI 写代码"到"AI 做软件工程"的工程化拐点——竞争点不是模型能力，是工程化标准。四类代表（工具无关的理念/开发模式，可套用于任意 Agent 工具）：Superpowers=强制方法论（brainstorm→writing-plans→TDD→code-review→subagent-driven，规则而非建议）；GSD=轻量 context 管理（解决 context rot，spec 驱动，.planning/.memory/.context）；GStack=虚拟工程团队（office-hours/CEO/Eng/Design/Staff/QA/Ship 角色分工，一个人跑完一个团队流程）；Trellis=仓库级记忆（Plan→Implement→Verify→Finish 四阶段，把规范/任务/记忆沉淀进仓库、每次 Finish 都为下次积累）。可组合：强制方法论+仓库记忆、轻量 context+虚拟团队。
```

### 2.7 产品视角总纲
#### 2.7.1 上下文优势
#### 2.7.2 判断力
#### 2.7.3 演示验证

```备注
人类的优势与其说是"品味"，不如说是上下文优势：用户是谁、业务边界、约束条件、竞品在做什么——这些还锁在人脑里。只要人还掌握 Agent 不知道的上下文，就必须留在 Loop 里把信息补给系统。Loop 放大的是你的判断力：你对系统理解多深，传感器就能写多精确。本知识库据此组织——先讲理念、原理、工程约束与设计，再用不同案例来演示、验证；每个案例都标注它演示了哪些原理、采用了哪套设计。
```

## 3. 系统架构设计（产品视角）

![产品视角架构设计流程](outputs/product_case_library/svg/fig_arch_flow.svg)

> 同一套方法论：从产品与业务出发，走完系统架构设计。架构不是技术选型清单，而是在特定约束下对质量属性做出的一组可追溯决策。（复用并优化自本仓库早期架构教程。）

### 3.1 约束先行
#### 3.1.1 业务约束
#### 3.1.2 合规约束
#### 3.1.3 规模约束

```备注
架构的起点不是技术，是约束。先把产品/业务的硬约束列清：业务边界与干系人诉求、合规与安全红线（如等保/日志留存/隐私）、规模与增长（并发、数据量、峰值）。约束表是后续每个决策的作证依据——没有约束支撑的复杂度，就是没有证据的复杂度。
```

### 3.2 质量属性
#### 3.2.1 性能场景
#### 3.2.2 可用性
#### 3.2.3 安全

```备注
把"非功能需求"写成可量化的质量属性场景（SEI 风格：刺激→环境→响应→度量），如"峰值 400 点/秒写入，P95<200ms"。两个功能清单相同的系统，可因质量属性优先级不同而导出完全不同的架构。质量属性场景是拆分与选型的证据来源。
```

### 3.3 子系统分解
#### 3.3.1 按数据流
#### 3.3.2 按变更率
#### 3.3.3 模块边界

```备注
拆分要有过硬证据：按数据流（写路径 vs 读路径）、按变更节奏（稳定的与频繁迭代的）、按故障与发布边界切分。模块化单体优先——模块边界就是预留的进程边界，"预留而不预付"。用架构守护测试把边界变成可执行的适应度函数，架构决策才不只是墙上的海报。
```

### 3.4 接口契约
#### 3.4.1 错误信封
#### 3.4.2 幂等
#### 3.4.3 契约即代码

```备注
接口是子系统间的合同：统一错误信封 {code,message,details}、写操作幂等、404 不泄露资源存在性、分页与 allowedActions 由服务端驱动。契约即代码——OpenAPI 由路由 schema 自动生成，契约与实现同源不漂移。
```

### 3.5 ADR 决策
#### 3.5.1 背景
#### 3.5.2 决策
#### 3.5.3 后果

```备注
每个关键决策写一篇 ADR（Nygard 格式）：背景（约束与证据）、决策（选了什么）、后果（正负影响、演进触发条件）。重点是"为什么这样选"，否则几周后仍会重复争论。决策可追溯，是产品视角架构的核心产物。
```

### 3.6 部署演进
#### 3.6.1 部署视图
#### 3.6.2 演进触发
#### 3.6.3 奥卡姆

```备注
部署图画的是约束表描述的真实机房，不是理想拓扑。用演进触发表把"何时该改造"写成可观察条件（规模×N、新需求立项…）提前量。奥卡姆原则贯穿：能用一张聚合表解决就不上专用集群，复杂度花在业务上，每个组件都要有证据。
```

## 4. 工程规范与代码约束

![工程规范与代码约束](outputs/product_case_library/svg/fig_engineering_rules.svg)

> 既是本教程的工程原理，也真实约束本仓库代码，并被 `verify_course_package.mjs` 守护。全文规范沉淀在 `rules/`（对齐 Google Style Guides、swe-book、Conventional Commits、OWASP 等，见 `rules/references.md`）。

### 4.1 复用优先
#### 4.1.1 成熟方案
#### 4.1.2 避免重复
#### 4.1.3 最小改动

```备注
优先调研成熟稳定的开源库，不重复造轮子；两处以上可复用逻辑抽离为公共模块（DRY）；优先在现有代码上扩展而非推倒重写、保持 API 兼容、改动范围最小化。见 rules/ai-dev-constraints.md 第 1/4/10 条。
```

### 4.2 结构清晰
#### 4.2.1 组件拆分
#### 4.2.2 目录规范
#### 4.2.3 分层架构

```备注
按功能拆分、单一职责，页面组织业务而不实现所有细节；新增代码遵循既定目录、不随意加顶级目录、工具/组件/接口/类型不混放；后端分层 controller→service，controller 不写业务、service 不碰 HTTP 上下文。见 rules/ai-dev-constraints.md 第 2/3 条与 rules/backend.md。
```

### 4.3 代码质量
#### 4.3.1 简洁<800行
#### 4.3.2 类型安全
#### 4.3.3 中文注释

```备注
可读性优先于炫技，一个函数只做一件事，不留废弃代码/调试输出；**单文件 < 800 行**，超出即拆分；优先 TS 类型、避免大量 any；统一中文注释，重点写"为什么这样做"与业务规则，复杂函数（约 30 行以上）前加整体思路。见 rules/ai-dev-constraints.md 第 5/6/7 条。
```

### 4.4 健壮与安全
#### 4.4.1 错误处理
#### 4.4.2 性能意识
#### 4.4.3 安全红线

```备注
所有异步请求做异常处理、不忽略 Promise 错误、用户可感知错误给明确提示；按需加载、避免不必要重复渲染；输入校验、防路径遍历、不硬编码密钥、敏感配置走环境变量。见 rules/ai-dev-constraints.md 第 8/9 条与 rules/backend.md 安全节（OWASP Top 10）。
```

### 4.5 前端规范
#### 4.5.1 Monorepo
#### 4.5.2 微前端
#### 4.5.3 公共沉淀

```备注
Monorepo（pnpm workspace）+ 微前端（qiankun）：shell 只做入口不写业务、业务放各子应用；公共能力沉淀 packages/（ui/theme/auth/request/utils），不重复封装请求/认证/主题；VITE_ 环境变量、独立构建。见 rules/frontend.md。
```

### 4.6 后端规范
#### 4.6.1 统一响应
#### 4.6.2 结构化日志
#### 4.6.3 优雅退出

```备注
统一响应 {code,message,data}、RESTful、按模块分组路由；结构化日志（slog）禁 printf 风格、语义化状态码；长任务用协程+channel、退出时清理资源、WebSocket 心跳超时。见 rules/backend.md。
```

## 5. 设计系统（多套，各不相同）

![多套设计系统](outputs/product_case_library/svg/fig_designs.svg)

> 不是一套统一审美：`design/` 下 ≥5 套**各不相同**的深色大屏设计系统（色板/布局/气质各异，对齐大屏参考与 voltagent DESIGN.md 模型）。不同案例采用不同设计 → 大屏风格各异。机器令牌单一来源 `design/themes.json`，人读文档 `design/<id>.md`。

### 5.1 石墨 HUD
#### 5.1.1 青蓝高光
#### 5.1.2 上图下表
#### 5.1.3 经营驾驶舱

```备注
石墨黑高端科技、青蓝(#22d3ee)高光、HUD 网格；顶部总览+中部趋势主图+底部明细表。适用 executive_dashboard/hr 类经营驾驶舱。见 design/graphite-hud.md。
```

### 5.2 翡翠流向
#### 5.2.1 翡翠发光
#### 5.2.2 桑基瀑布
#### 5.2.3 流程流向

```备注
近黑墨绿、翡翠(#10b981)发光、流向感；桑基/瀑布纵向布局。适用 workflow_pipeline/knowledge 类流程流向。见 design/emerald-flow.md。
```

### 5.3 琥珀漏斗
#### 5.3.1 暖金高光
#### 5.3.2 漏斗级联
#### 5.3.3 转化收束

```备注
深棕琥珀、暖金(#f59e0b)高光、转化收束；漏斗级联布局。适用 sales_funnel 类转化分析。见 design/amber-funnel.md。
```

### 5.4 青紫矩阵
#### 5.4.1 双色高光
#### 5.4.2 矩阵热力
#### 5.4.3 密集决策

```备注
墨蓝、青(#06b6d4)与品紫(#a855f7)双高光、密集矩阵；矩阵/热力/散点布局。适用 matrix_decision/finance/harness/configuration。见 design/cyan-matrix.md。
```

### 5.5 钢灰队列
#### 5.5.1 冷蓝高光
#### 5.5.2 复核队列
#### 5.5.3 明细留痕

```备注
钢灰近黑、冷蓝(#60a5fa)高光、明细与复核；复核队列/明细表布局。适用 review_queue/list_review/record_detail。见 design/steel-queue.md。
```

## 使用入口

- 案例总览与 5 个 manifest：`outputs/product_case_library/*.json`
- 多套设计系统：`design/*.md`（令牌 `design/themes.json`）｜工程规范：`rules/`｜Skill 与 Loop：`skills/`
- React 工作台：`code/web`（`npm ci && npm run build && npm run preview`，一案例一路由 `#/case/NN`）
- 数据：`node code/tools/fetch-datasets.mjs`｜预计算：`node code/tools/build_case_data.mjs`｜校验：`node code/tools/verify_course_package.mjs`
- **统一运行/纠错约定**：`bash code/run.sh` 起一个服务（Fastify+node:sqlite 托管 API+前端），浏览器 `#/case/NN` 即真后端实时数据；遵 `rules/`（DRY / 单文件<800 行 / 类型 / 中文注释）；`verify_course_package.mjs` 逐项核验。以下每个案例不再重复这段。

## 术语表（先备着，看案例时随时回查）

| 术语 | 一句话 |
|---|---|
| Token | 大模型处理文本的最小计量单位；1 Token≈1.5~2 汉字（§1.2） |
| Context / 上下文窗口 | 单次运算的全部输入 / 其最大 Token 容量（§1.3） |
| RAG | 检索增强生成：分片→索引→召回→重排→生成，只喂高相关片段（§1.3） |
| Embedding / 向量库 | 把文本转成向量 / 存向量+原文、按相似度检索（§1.3、案例44） |
| Agent / ReAct | 自主分步的智能体 / 思考→调用工具→观察→再思考的循环（§1.6） |
| systemLayer / systemStage | 案例在数字化系统的「层（底座/能力/应用）/ 环节（采集→…→增长）」 |
| metricSpec | 案例指标的真实列计算规格（保证 KPI 真算、可溯源、非编造） |
| 难度 | 入门 / 进阶 / 高阶，标在每个案例头部 |

# 第二部分 · 案例演示与验证

## 数字化系统全景（先看这张图）

第一部分讲的理念、原理、规范、设计，不是散点——它们共同构成**一套数字化系统**。后面 25 个案例，正是这套系统在不同环节、不同层的**实操演示**：

![数字化系统全景](outputs/product_case_library/svg/fig_system_panorama.svg)

- **纵向三层**：`底座平台`（数据接入/存储/治理/架构，44 向量库·45 关系库·46 架构契约·47 三维）→ `能力智能`（指标/检索/AI）→ `业务应用`（业务场景）。底座支撑上层。
- **横向数据价值闭环**：`采集接入 → 治理存储 → 洞察分析 → 决策研判 → 执行落地 → 质量验收 → 增长闭环`，再反馈回采集。每个业务案例都是这条闭环上的一个节点。
- **怎么读**：先在全景里定位一个案例在「哪一层·哪一环节」，再进它的章节看它把前面**哪条理论落成了什么实际操作**。

## 案例总览

| # | 场景 | 行业 | 阶段 | 演示原理 | 设计 | UI 原型 | Skill |
|---|---|---|---|---|---|---|---|
| 01 | 电商早会异常订单台 | 电商零售 | 角色转型 | 2.1/2.7 | graphite-hud | `ui_01_morning_ops_grid` | problem-framing+metric-definition+acceptance-criteria |
| 06 | 银行客服知识治理 | 银行客服 | AI协作 | 2.4/2.6 | emerald-flow | `ui_06_bank_knowledge_governance` | ai-product-boundary+eval-design+human-review |
| 14 | 物流异常派单扩城 | 物流配送 | 需求管理 | 3.4/4.1 | amber-funnel | `ui_14_logistics_change_control` | change-impact+release-risk+rollback-plan |
| 16 | 医院预约容量调度 | 医疗运营 | 用户洞察 | 2.7/3.2 | graphite-hud | `ui_16_hospital_capacity_scheduler` | persona-scenario+journey-map+human-review |
| 28 | 金融复核工作台 | 金融风控 | 详细设计 | 3.3/3.5 | cyan-matrix | `ui_28_finance_review_queue` | risk-rule-design+human-review+compliance-boundary |
| 30 | 航空会员价值运营 | 航空会员 | 数据指标 | 3.2/4.3 | amber-funnel | `ui_30_airline_member_rfm` | rfm-segmentation+lifecycle-action+privacy-boundary |
| 31 | 广告投放转化复盘 | 广告投放 | 数据指标 | 3.2/4.3 | amber-funnel | `ui_31_ad_conversion_funnel` | funnel-analysis+budget-decision+metric-definition |
| 41 | 零售经营产品方案 | 零售经营 | 综合闭环 | 2.7/3.1/4.1 | graphite-hud | `ui_41_retail_capstone_board` | capstone-product-flow+evidence-pack+traceability-check |
| 44 | 产品知识库语义检索 | AI 应用 | AI协作 | 1.3/3.3 | emerald-flow | `ui_44_rag_retrieval` | capstone-product-flow+evidence-pack+traceability-check |
| 45 | 经营数据关系库查询 | 数据工程 | 系统架构 | 3.3/4.1 | steel-queue | `ui_45_pg_relational` | capstone-product-flow+evidence-pack+traceability-check |
| 46 | 后端子系统分解与契约 | 系统架构 | 系统架构 | 3.1/3.3 | cyan-matrix | `ui_46_arch_contract` | capstone-product-flow+evidence-pack+traceability-check |
| 47 | 经营三维散点可视化 | 数据可视化 | 数据指标 | 3.3/5.1 | graphite-hud | `ui_47_three_scatter` | capstone-product-flow+evidence-pack+traceability-check |


---

## 实操 01：技术任务转产品问题｜电商早会异常订单台

> **本案例演示/验证**：原理 2.1、2.7｜**采用设计** `graphite-hud`（见 design/graphite-hud.md）

> **在数字化系统中的位置**：业务应用层 · 洞察环节｜**理论→实操**：把原理 2.1、2.7 落成可运行操作：识别未达成区域、定位异常业务线，并生成责任部门行动队列。（底座依赖 44/45/46/47）

> 🎚 **难度** 入门｜🎯 **一句话** 电商早会异常订单台：识别未达成区域、定位异常业务线，并生成责任部门行动队列
>
> 💡 **洞见**：别只做「executive_dashboard」页面——把「订单数」等指标 → 异常 → 责任 → 行动串成闭环，落在 洞察 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得把查询慢等同于产品问题」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

电商零售场景中，运营主管围绕异常订单和高风险 SKU做日常判断。运营主管需要围绕“异常订单和高风险 SKU”完成“在早会前完成风险识别”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：订单数、销售额(元)、毛利率均值、异常订单率、区域数。
- 现场常见异常：目标未达成、区域下滑、异常订单、责任未闭环。
- 只做通用页面无法支撑「识别未达成区域、定位异常业务线，并生成责任部门行动队列。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `problem-framing` 与 `metric-definition` 完成分析，产出 `产品问题定义卡`，用 `acceptance-criteria` 验收。

### 任务目标与数据

- 行业：电商零售
- 真实业务场景：电商早会异常订单台
- 岗位：运营主管
- 数据或资料：`dataset/order_data.csv`（1200 行，异常 225）
- 公开参考：https://archive.ics.uci.edu/dataset/352/online+retail
- 行业字段：订单号、SKU、库存天数、毛利率、异常原因、责任人、处理时限
- 指标链（真实值）：订单数 1200，销售额(元) 16630819，毛利率均值 33.03%，异常订单率 18.8%，区域数 6
- 决策动作：识别未达成区域、定位异常业务线，并生成责任部门行动队列。
- 风险边界：不得把查询慢等同于产品问题
- UI 原型：`ui_01_morning_ops_grid`（executive_dashboard）
- 采用设计：graphite-hud
- SaaS 组件：目标达成、收入趋势、业务结构、区域排行、异常告警、行动队列

### Prompt 实操

**Prompt 1：电商早会异常订单台 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「电商早会异常订单台」的产品问题定义，并产出可运行原型：
- 场景与岗位：运营主管 面向「电商早会异常订单台」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（订单号、SKU、库存天数、毛利率、异常原因、责任人、处理时限）。
- 指标链：订单数、销售额(元)、毛利率均值、异常订单率、区域数（当前真实值：订单数=1200，销售额(元)=16630819，毛利率均值=33.03%，异常订单率=18.8%，区域数=6）。
- 异常状态：目标未达成、区域下滑、异常订单、责任未闭环。
- 使用 Skill：problem-framing、metric-definition，并用 acceptance-criteria 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/01`，按 `ui_01_morning_ops_grid`（executive_dashboard）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：产品问题定义卡，保存为 `outputs/product_case_library/case_01_morning_ops_grid_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得把查询慢等同于产品问题」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：电商早会异常订单台 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「电商早会异常订单台」的方案验收，并产出可运行原型：
- 场景与岗位：运营主管 面向「电商早会异常订单台」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（订单号、SKU、库存天数、毛利率、异常原因、责任人、处理时限）。
- 指标链：订单数、销售额(元)、毛利率均值、异常订单率、区域数（当前真实值：订单数=1200，销售额(元)=16630819，毛利率均值=33.03%，异常订单率=18.8%，区域数=6）。
- 异常状态：目标未达成、区域下滑、异常订单、责任未闭环。
- 使用 Skill：problem-framing、metric-definition，并用 acceptance-criteria 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/01`，按 `ui_01_morning_ops_grid`（executive_dashboard）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：产品问题定义卡，保存为 `outputs/product_case_library/case_01_morning_ops_grid_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得把查询慢等同于产品问题」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![电商早会异常订单台 · 信息图](outputs/product_case_library/svg/case_01_morning_ops_grid.svg)

![电商早会异常订单台 · 可运行大屏原型截图](assets/screenshots/premium_case_01_morning_ops_grid_desktop.png)

- 图形类型：morning_ops_grid（设计 graphite-hud）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_01_morning_ops_grid` + 设计 `graphite-hud`，不得复用通用表格占位；可运行原型见 `#/case/01`。

### 交付物与验收

- 交付物：产品问题定义卡
- 必含字段：订单号、SKU、库存天数、毛利率、异常原因、责任人、处理时限
- 必含指标链：订单数、销售额(元)、毛利率均值、异常订单率、区域数
- 必含异常状态：目标未达成、区域下滑、异常订单、责任未闭环
- 必含 Skill：problem-framing、metric-definition、acceptance-criteria

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得把查询慢等同于产品问题」。
- 交付物文件：`outputs/product_case_library/case_01_morning_ops_grid_问题定义.md`、`…_方案验收.md`。


**指定实操融合**

- RP01：同一条软件需求的三种角色表达
  - 产出：角色差异卡, 产品问题清单
  - 验收：同一需求必须分别呈现研发视角、项目经理视角和产品经理视角，并能指出产品问题。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/01`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 06：AI 产品经理工作流｜银行客服知识治理

> **本案例演示/验证**：原理 2.4、2.6｜**采用设计** `emerald-flow`（见 design/emerald-flow.md）

> **在数字化系统中的位置**：能力智能层 · 治理环节｜**理论→实操**：把原理 2.4、2.6 落成可运行操作：围绕银行客服知识库判断优先级、责任人和验收动作。（数字化底座本身）

> 🎚 **难度** 高阶｜🎯 **一句话** 银行客服知识治理：围绕银行客服知识库判断优先级、责任人和验收动作
>
> 💡 **洞见**：别只做「knowledge_workspace」页面——把「交易数」等指标 → 异常 → 责任 → 行动串成闭环，落在 治理 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得输出自动授信或最终处置建议」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

银行客服场景中，AI 产品经理围绕银行客服知识库做日常判断。AI 产品经理需要围绕“银行客服知识库”完成“治理问答边界和上线评估”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：交易数、高风险率、已复核率、渠道数、平均金额(元)。
- 现场常见异常：字段缺失、流程超时、复核未完成、验收失败。
- 只做通用页面无法支撑「围绕银行客服知识库判断优先级、责任人和验收动作。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `ai-product-boundary` 与 `eval-design` 完成分析，产出 `AI 产品工作流蓝图`，用 `human-review` 验收。

### 任务目标与数据

- 行业：银行客服
- 真实业务场景：银行客服知识治理
- 岗位：AI 产品经理
- 数据或资料：`dataset/product_cases/aicourse_financial_transactions.csv`（900 行，异常 592）
- 公开参考：https://github.com/VoltAgent/awesome-agent-skills
- 行业字段：问题类别、知识来源、命中率、风险答案、人工接管、留痕编号
- 指标链（真实值）：交易数 900，高风险率 31.1%，已复核率 0%，渠道数 4，平均金额(元) 39999.6
- 决策动作：围绕银行客服知识库判断优先级、责任人和验收动作。
- 风险边界：不得输出自动授信或最终处置建议（高影响行业·人工复核）
- UI 原型：`ui_06_bank_knowledge_governance`（knowledge_workspace）
- 采用设计：emerald-flow
- SaaS 组件：知识目录、任务地图、问答记录、命中材料、错题复盘、能力画像

### Prompt 实操

**Prompt 1：银行客服知识治理 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「银行客服知识治理」的产品问题定义，并产出可运行原型：
- 场景与岗位：AI 产品经理 面向「银行客服知识治理」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_financial_transactions.csv`，只使用数据/资料中存在的字段（问题类别、知识来源、命中率、风险答案、人工接管、留痕编号）。
- 指标链：交易数、高风险率、已复核率、渠道数、平均金额(元)（当前真实值：交易数=900，高风险率=31.1%，已复核率=0%，渠道数=4，平均金额(元)=39999.6）。
- 异常状态：字段缺失、流程超时、复核未完成、验收失败。
- 使用 Skill：ai-product-boundary、eval-design，并用 human-review 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/06`，按 `ui_06_bank_knowledge_governance`（knowledge_workspace）与设计 `emerald-flow` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：AI 产品工作流蓝图，保存为 `outputs/product_case_library/case_06_bank_knowledge_governance_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://github.com/VoltAgent/awesome-agent-skills）；不得越过「不得输出自动授信或最终处置建议」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：银行客服知识治理 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「银行客服知识治理」的方案验收，并产出可运行原型：
- 场景与岗位：AI 产品经理 面向「银行客服知识治理」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_financial_transactions.csv`，只使用数据/资料中存在的字段（问题类别、知识来源、命中率、风险答案、人工接管、留痕编号）。
- 指标链：交易数、高风险率、已复核率、渠道数、平均金额(元)（当前真实值：交易数=900，高风险率=31.1%，已复核率=0%，渠道数=4，平均金额(元)=39999.6）。
- 异常状态：字段缺失、流程超时、复核未完成、验收失败。
- 使用 Skill：ai-product-boundary、eval-design，并用 human-review 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/06`，按 `ui_06_bank_knowledge_governance`（knowledge_workspace）与设计 `emerald-flow` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：AI 产品工作流蓝图，保存为 `outputs/product_case_library/case_06_bank_knowledge_governance_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://github.com/VoltAgent/awesome-agent-skills）；不得越过「不得输出自动授信或最终处置建议」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![银行客服知识治理 · 信息图](outputs/product_case_library/svg/case_06_bank_knowledge_governance.svg)

![银行客服知识治理 · 可运行大屏原型截图](assets/screenshots/premium_case_06_bank_knowledge_governance_desktop.png)

- 图形类型：bank_knowledge_governance（设计 emerald-flow）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_06_bank_knowledge_governance` + 设计 `emerald-flow`，不得复用通用表格占位；可运行原型见 `#/case/06`。

### 交付物与验收

- 交付物：AI 产品工作流蓝图
- 必含字段：问题类别、知识来源、命中率、风险答案、人工接管、留痕编号
- 必含指标链：交易数、高风险率、已复核率、渠道数、平均金额(元)
- 必含异常状态：字段缺失、流程超时、复核未完成、验收失败
- 必含 Skill：ai-product-boundary、eval-design、human-review

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得输出自动授信或最终处置建议」。
- 交付物文件：`outputs/product_case_library/case_06_bank_knowledge_governance_问题定义.md`、`…_方案验收.md`。


**指定实操融合**

- RP13：本地资料知识库问答流程
  - 产出：知识库配置记录, 问答测试记录, 召回改进清单
  - 验收：至少测试 3 个问题，记录命中材料、答案质量和改进点。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/06`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 14：需求变更控制｜物流异常派单扩城

> **本案例演示/验证**：原理 3.4、4.1｜**采用设计** `amber-funnel`（见 design/amber-funnel.md）

> **在数字化系统中的位置**：业务应用层 · 决策环节｜**理论→实操**：把原理 3.4、4.1 落成可运行操作：判断漏斗断点、回款风险和区域销售动作优先级。（底座依赖 44/45/46/47）

> 🎚 **难度** 进阶｜🎯 **一句话** 物流异常派单扩城：判断漏斗断点、回款风险和区域销售动作优先级
>
> 💡 **洞见**：别只做「sales_funnel_screen」页面——把「运单数」等指标 → 异常 → 责任 → 行动串成闭环，落在 决策 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得在无回滚条件下扩大范围」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

物流配送场景中，物流产品经理围绕异常派单扩城做日常判断。物流产品经理需要围绕“异常派单扩城”完成“评估需求变更的影响和回滚策略”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：运单数、异常率、扩城率、城市数、平均实际时效(h)。
- 现场常见异常：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 只做通用页面无法支撑「判断漏斗断点、回款风险和区域销售动作优先级。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `change-impact` 与 `release-risk` 完成分析，产出 `需求变更影响评估`，用 `rollback-plan` 验收。

### 任务目标与数据

- 行业：物流配送
- 真实业务场景：物流异常派单扩城
- 岗位：物流产品经理
- 数据或资料：`dataset/product_cases/aicourse_logistics_delivery.csv`（700 行，异常 541）
- 公开参考：https://onfleet.com/blog/dispatch-management/
- 行业字段：城市、运单、异常类型、规则版本、客服话术、回滚条件
- 指标链（真实值）：运单数 700，异常率 77.3%，扩城率 23.6%，城市数 8，平均实际时效(h) 62.96
- 决策动作：判断漏斗断点、回款风险和区域销售动作优先级。
- 风险边界：不得在无回滚条件下扩大范围
- UI 原型：`ui_14_logistics_change_control`（sales_funnel_screen）
- 采用设计：amber-funnel
- SaaS 组件：销售漏斗、目标进度、客户跟进、回款风险、区域作战、销售排行

### Prompt 实操

**Prompt 1：物流异常派单扩城 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「物流异常派单扩城」的产品问题定义，并产出可运行原型：
- 场景与岗位：物流产品经理 面向「物流异常派单扩城」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_logistics_delivery.csv`，只使用数据/资料中存在的字段（城市、运单、异常类型、规则版本、客服话术、回滚条件）。
- 指标链：运单数、异常率、扩城率、城市数、平均实际时效(h)（当前真实值：运单数=700，异常率=77.3%，扩城率=23.6%，城市数=8，平均实际时效(h)=62.96）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：change-impact、release-risk，并用 rollback-plan 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/14`，按 `ui_14_logistics_change_control`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：需求变更影响评估，保存为 `outputs/product_case_library/case_14_logistics_change_control_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://onfleet.com/blog/dispatch-management/）；不得越过「不得在无回滚条件下扩大范围」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：物流异常派单扩城 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「物流异常派单扩城」的方案验收，并产出可运行原型：
- 场景与岗位：物流产品经理 面向「物流异常派单扩城」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_logistics_delivery.csv`，只使用数据/资料中存在的字段（城市、运单、异常类型、规则版本、客服话术、回滚条件）。
- 指标链：运单数、异常率、扩城率、城市数、平均实际时效(h)（当前真实值：运单数=700，异常率=77.3%，扩城率=23.6%，城市数=8，平均实际时效(h)=62.96）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：change-impact、release-risk，并用 rollback-plan 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/14`，按 `ui_14_logistics_change_control`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：需求变更影响评估，保存为 `outputs/product_case_library/case_14_logistics_change_control_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://onfleet.com/blog/dispatch-management/）；不得越过「不得在无回滚条件下扩大范围」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![物流异常派单扩城 · 信息图](outputs/product_case_library/svg/case_14_logistics_change_control.svg)

![物流异常派单扩城 · 可运行大屏原型截图](assets/screenshots/premium_case_14_logistics_change_control_desktop.png)

- 图形类型：logistics_change_control（设计 amber-funnel）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_14_logistics_change_control` + 设计 `amber-funnel`，不得复用通用表格占位；可运行原型见 `#/case/14`。

### 交付物与验收

- 交付物：需求变更影响评估
- 必含字段：城市、运单、异常类型、规则版本、客服话术、回滚条件
- 必含指标链：运单数、异常率、扩城率、城市数、平均实际时效(h)
- 必含异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队
- 必含 Skill：change-impact、release-risk、rollback-plan

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得在无回滚条件下扩大范围」。
- 交付物文件：`outputs/product_case_library/case_14_logistics_change_control_问题定义.md`、`…_方案验收.md`。


**指定实操融合**

- RP03：产品评审会协作纪要
  - 产出：产品评审纪要, 验收口径清单
  - 验收：纪要必须覆盖需求、范围、风险、验收口径和责任人。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/14`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 16：目标用户与场景｜医院预约容量调度

> **本案例演示/验证**：原理 2.7、3.2｜**采用设计** `graphite-hud`（见 design/graphite-hud.md）

> **在数字化系统中的位置**：业务应用层 · 洞察环节｜**理论→实操**：把原理 2.7、3.2 落成可运行操作：判断岗位产出、编制缺口和流失风险，形成组织调整建议。（底座依赖 44/45/46/47）

> 🎚 **难度** 入门｜🎯 **一句话** 医院预约容量调度：判断岗位产出、编制缺口和流失风险，形成组织调整建议
>
> 💡 **洞见**：别只做「hr_efficiency_screen」页面——把「就诊数」等指标 → 异常 → 责任 → 行动串成闭环，落在 洞察 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得自动诊断或替代医生决策」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

医疗运营场景中，医疗运营产品经理围绕预约容量调度做日常判断。医疗运营产品经理需要围绕“预约容量调度”完成“平衡患者等待和医护资源”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：就诊数、号源利用率均值、爽约率、科室数、平均等待(分)。
- 现场常见异常：招聘延期、人效偏低、编制超配、流失预警。
- 只做通用页面无法支撑「判断岗位产出、编制缺口和流失风险，形成组织调整建议。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `persona-scenario` 与 `journey-map` 完成分析，产出 `目标用户与场景矩阵`，用 `human-review` 验收。

### 任务目标与数据

- 行业：医疗运营
- 真实业务场景：医院预约容量调度
- 岗位：医疗运营产品经理
- 数据或资料：`dataset/product_cases/aicourse_healthcare_diabetes.csv`（520 行，异常 61）
- 公开参考：https://github.com/vintasoftware/medplum-ai-concierge
- 行业字段：科室、医生、预约槽、等待时间、加号规则、人工协调
- 指标链（真实值）：就诊数 520，号源利用率均值 69.54%，爽约率 11.7%，科室数 5，平均等待(分) 95.87
- 决策动作：判断岗位产出、编制缺口和流失风险，形成组织调整建议。
- 风险边界：不得自动诊断或替代医生决策（高影响行业·人工复核）
- UI 原型：`ui_16_hospital_capacity_scheduler`（hr_efficiency_screen）
- 采用设计：graphite-hud
- SaaS 组件：人效产出、招聘进度、组织编制、薪酬成本、流失预警、岗位效率

### Prompt 实操

**Prompt 1：医院预约容量调度 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「医院预约容量调度」的产品问题定义，并产出可运行原型：
- 场景与岗位：医疗运营产品经理 面向「医院预约容量调度」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_healthcare_diabetes.csv`，只使用数据/资料中存在的字段（科室、医生、预约槽、等待时间、加号规则、人工协调）。
- 指标链：就诊数、号源利用率均值、爽约率、科室数、平均等待(分)（当前真实值：就诊数=520，号源利用率均值=69.54%，爽约率=11.7%，科室数=5，平均等待(分)=95.87）。
- 异常状态：招聘延期、人效偏低、编制超配、流失预警。
- 使用 Skill：persona-scenario、journey-map，并用 human-review 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/16`，按 `ui_16_hospital_capacity_scheduler`（hr_efficiency_screen）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：目标用户与场景矩阵，保存为 `outputs/product_case_library/case_16_hospital_capacity_scheduler_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://github.com/vintasoftware/medplum-ai-concierge）；不得越过「不得自动诊断或替代医生决策」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：医院预约容量调度 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「医院预约容量调度」的方案验收，并产出可运行原型：
- 场景与岗位：医疗运营产品经理 面向「医院预约容量调度」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/product_cases/aicourse_healthcare_diabetes.csv`，只使用数据/资料中存在的字段（科室、医生、预约槽、等待时间、加号规则、人工协调）。
- 指标链：就诊数、号源利用率均值、爽约率、科室数、平均等待(分)（当前真实值：就诊数=520，号源利用率均值=69.54%，爽约率=11.7%，科室数=5，平均等待(分)=95.87）。
- 异常状态：招聘延期、人效偏低、编制超配、流失预警。
- 使用 Skill：persona-scenario、journey-map，并用 human-review 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/16`，按 `ui_16_hospital_capacity_scheduler`（hr_efficiency_screen）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：目标用户与场景矩阵，保存为 `outputs/product_case_library/case_16_hospital_capacity_scheduler_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://github.com/vintasoftware/medplum-ai-concierge）；不得越过「不得自动诊断或替代医生决策」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![医院预约容量调度 · 信息图](outputs/product_case_library/svg/case_16_hospital_capacity_scheduler.svg)

![医院预约容量调度 · 可运行大屏原型截图](assets/screenshots/premium_case_16_hospital_capacity_scheduler_desktop.png)

- 图形类型：hospital_capacity_scheduler（设计 graphite-hud）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_16_hospital_capacity_scheduler` + 设计 `graphite-hud`，不得复用通用表格占位；可运行原型见 `#/case/16`。

### 交付物与验收

- 交付物：目标用户与场景矩阵
- 必含字段：科室、医生、预约槽、等待时间、加号规则、人工协调
- 必含指标链：就诊数、号源利用率均值、爽约率、科室数、平均等待(分)
- 必含异常状态：招聘延期、人效偏低、编制超配、流失预警
- 必含 Skill：persona-scenario、journey-map、human-review

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得自动诊断或替代医生决策」。
- 交付物文件：`outputs/product_case_library/case_16_hospital_capacity_scheduler_问题定义.md`、`…_方案验收.md`。


**指定实操融合**

- RP04：用户体验五层与服务蓝图
  - 产出：用户体验地图, 服务蓝图, 用户故事, 体验五层拆解
  - 验收：体验地图、用户故事和服务蓝图必须描述同一个用户任务。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/16`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 28：权限与风控规则｜金融复核工作台

> **本案例演示/验证**：原理 3.3、3.5｜**采用设计** `cyan-matrix`（见 design/cyan-matrix.md）

> **在数字化系统中的位置**：业务应用层 · 执行环节｜**理论→实操**：把原理 3.3、3.5 落成可运行操作：识别现金流缺口、逾期应收和预算偏差，形成复核与催收动作。（底座依赖 44/45/46/47）

> 🎚 **难度** 进阶｜🎯 **一句话** 金融复核工作台：识别现金流缺口、逾期应收和预算偏差，形成复核与催收动作
>
> 💡 **洞见**：别只做「finance_cashflow_screen」页面——把「交易数」等指标 → 异常 → 责任 → 行动串成闭环，落在 执行 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得自动处罚、不得自动授信、不得自动拒绝交易」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

金融风控场景中，风控产品经理围绕金融交易复核做日常判断。风控产品经理需要围绕“金融交易复核”完成“设计人工复核与权限边界”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：交易数、高风险率、已复核率、平均命中规则数、渠道数。
- 现场常见异常：高风险待复核、证据不足、权限越界、超时未处理。
- 只做通用页面无法支撑「识别现金流缺口、逾期应收和预算偏差，形成复核与催收动作。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `risk-rule-design` 与 `human-review` 完成分析，产出 `权限矩阵与风控规则`，用 `compliance-boundary` 验收。

### 任务目标与数据

- 行业：金融风控
- 真实业务场景：金融复核工作台
- 岗位：风控产品经理
- 数据或资料：`dataset/reference_data_analysis/28-creditcardfraud_sample.csv`（600 行，异常 50）
- 公开参考：https://archive.ics.uci.edu/dataset/222/bank+marketing
- 行业字段：交易号、账户信号、风险等级、命中规则、证据摘要、复核权限、复核结论、留痕
- 指标链（真实值）：交易数 600，高风险率 8.3%，已复核率 0%，平均命中规则数 0.25，渠道数 3
- 决策动作：识别现金流缺口、逾期应收和预算偏差，形成复核与催收动作。
- 风险边界：不得自动处罚、不得自动授信、不得自动拒绝交易（高影响行业·人工复核）
- UI 原型：`ui_28_finance_review_queue`（finance_cashflow_screen）
- 采用设计：cyan-matrix
- SaaS 组件：收入利润、成本费用、现金流、应收账款、预算执行、经营质量

### Prompt 实操

**Prompt 1：金融复核工作台 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「金融复核工作台」的产品问题定义，并产出可运行原型：
- 场景与岗位：风控产品经理 面向「金融复核工作台」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/28-creditcardfraud_sample.csv`，只使用数据/资料中存在的字段（交易号、账户信号、风险等级、命中规则、证据摘要、复核权限、复核结论、留痕）。
- 指标链：交易数、高风险率、已复核率、平均命中规则数、渠道数（当前真实值：交易数=600，高风险率=8.3%，已复核率=0%，平均命中规则数=0.25，渠道数=3）。
- 异常状态：高风险待复核、证据不足、权限越界、超时未处理。
- 使用 Skill：risk-rule-design、human-review，并用 compliance-boundary 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/28`，按 `ui_28_finance_review_queue`（finance_cashflow_screen）与设计 `cyan-matrix` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：权限矩阵与风控规则，保存为 `outputs/product_case_library/case_28_finance_review_queue_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/222/bank+marketing）；不得越过「不得自动处罚、不得自动授信、不得自动拒绝交易」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：金融复核工作台 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「金融复核工作台」的方案验收，并产出可运行原型：
- 场景与岗位：风控产品经理 面向「金融复核工作台」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/28-creditcardfraud_sample.csv`，只使用数据/资料中存在的字段（交易号、账户信号、风险等级、命中规则、证据摘要、复核权限、复核结论、留痕）。
- 指标链：交易数、高风险率、已复核率、平均命中规则数、渠道数（当前真实值：交易数=600，高风险率=8.3%，已复核率=0%，平均命中规则数=0.25，渠道数=3）。
- 异常状态：高风险待复核、证据不足、权限越界、超时未处理。
- 使用 Skill：risk-rule-design、human-review，并用 compliance-boundary 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/28`，按 `ui_28_finance_review_queue`（finance_cashflow_screen）与设计 `cyan-matrix` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：权限矩阵与风控规则，保存为 `outputs/product_case_library/case_28_finance_review_queue_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/222/bank+marketing）；不得越过「不得自动处罚、不得自动授信、不得自动拒绝交易」；高影响行业保留人工复核；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![金融复核工作台 · 信息图](outputs/product_case_library/svg/case_28_finance_review_queue.svg)

![金融复核工作台 · 可运行大屏原型截图](assets/screenshots/premium_case_28_finance_review_queue_desktop.png)

- 图形类型：finance_review_queue（设计 cyan-matrix）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_28_finance_review_queue` + 设计 `cyan-matrix`，不得复用通用表格占位；可运行原型见 `#/case/28`。

### 交付物与验收

- 交付物：权限矩阵与风控规则
- 必含字段：交易号、账户信号、风险等级、命中规则、证据摘要、复核权限、复核结论、留痕
- 必含指标链：交易数、高风险率、已复核率、平均命中规则数、渠道数
- 必含异常状态：高风险待复核、证据不足、权限越界、超时未处理
- 必含 Skill：risk-rule-design、human-review、compliance-boundary

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得自动处罚、不得自动授信、不得自动拒绝交易」。
- 交付物文件：`outputs/product_case_library/case_28_finance_review_queue_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/28`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 30：RFM 客户分层｜航空会员价值运营

> **本案例演示/验证**：原理 3.2、4.3｜**采用设计** `amber-funnel`（见 design/amber-funnel.md）

> **在数字化系统中的位置**：能力智能层 · 洞察环节｜**理论→实操**：把原理 3.2、4.3 落成可运行操作：判断漏斗断点、回款风险和区域销售动作优先级。（数字化底座本身）

> 🎚 **难度** 进阶｜🎯 **一句话** 航空会员价值运营：判断漏斗断点、回款风险和区域销售动作优先级
>
> 💡 **洞见**：别只做「sales_funnel_screen」页面——把「会员数」等指标 → 异常 → 责任 → 行动串成闭环，落在 洞察 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得输出歧视性或不可解释规则」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

航空会员场景中，会员产品经理围绕航空会员价值做日常判断。会员产品经理需要围绕“航空会员价值”完成“用分层指导差异化运营”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：会员数、年飞行次数均值、年消费总额(元)、高价值分层占比、里程余额均值。
- 现场常见异常：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 只做通用页面无法支撑「判断漏斗断点、回款风险和区域销售动作优先级。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `rfm-segmentation` 与 `lifecycle-action` 完成分析，产出 `RFM 分层运营策略`，用 `privacy-boundary` 验收。

### 任务目标与数据

- 行业：航空会员
- 真实业务场景：航空会员价值运营
- 岗位：会员产品经理
- 数据或资料：`dataset/reference_data_analysis/2-air_data.csv`（800 行，异常 10）
- 公开参考：https://archive.ics.uci.edu/dataset/352/online+retail
- 行业字段：会员号、最近飞行、飞行频次、里程、客舱、运营动作
- 指标链（真实值）：会员数 800，年飞行次数均值 25.93，年消费总额(元) 48741887，高价值分层占比 8.6%，里程余额均值 100169.12
- 决策动作：判断漏斗断点、回款风险和区域销售动作优先级。
- 风险边界：不得输出歧视性或不可解释规则
- UI 原型：`ui_30_airline_member_rfm`（sales_funnel_screen）
- 采用设计：amber-funnel
- SaaS 组件：销售漏斗、目标进度、客户跟进、回款风险、区域作战、销售排行

### Prompt 实操

**Prompt 1：航空会员价值运营 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「航空会员价值运营」的产品问题定义，并产出可运行原型：
- 场景与岗位：会员产品经理 面向「航空会员价值运营」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/2-air_data.csv`，只使用数据/资料中存在的字段（会员号、最近飞行、飞行频次、里程、客舱、运营动作）。
- 指标链：会员数、年飞行次数均值、年消费总额(元)、高价值分层占比、里程余额均值（当前真实值：会员数=800，年飞行次数均值=25.93，年消费总额(元)=48741887，高价值分层占比=8.6%，里程余额均值=100169.12）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：rfm-segmentation、lifecycle-action，并用 privacy-boundary 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/30`，按 `ui_30_airline_member_rfm`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：RFM 分层运营策略，保存为 `outputs/product_case_library/case_30_airline_member_rfm_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得输出歧视性或不可解释规则」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：航空会员价值运营 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「航空会员价值运营」的方案验收，并产出可运行原型：
- 场景与岗位：会员产品经理 面向「航空会员价值运营」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/2-air_data.csv`，只使用数据/资料中存在的字段（会员号、最近飞行、飞行频次、里程、客舱、运营动作）。
- 指标链：会员数、年飞行次数均值、年消费总额(元)、高价值分层占比、里程余额均值（当前真实值：会员数=800，年飞行次数均值=25.93，年消费总额(元)=48741887，高价值分层占比=8.6%，里程余额均值=100169.12）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：rfm-segmentation、lifecycle-action，并用 privacy-boundary 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/30`，按 `ui_30_airline_member_rfm`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：RFM 分层运营策略，保存为 `outputs/product_case_library/case_30_airline_member_rfm_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得输出歧视性或不可解释规则」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![航空会员价值运营 · 信息图](outputs/product_case_library/svg/case_30_airline_member_rfm.svg)

![航空会员价值运营 · 可运行大屏原型截图](assets/screenshots/premium_case_30_airline_member_rfm_desktop.png)

- 图形类型：airline_member_rfm（设计 amber-funnel）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_30_airline_member_rfm` + 设计 `amber-funnel`，不得复用通用表格占位；可运行原型见 `#/case/30`。

### 交付物与验收

- 交付物：RFM 分层运营策略
- 必含字段：会员号、最近飞行、飞行频次、里程、客舱、运营动作
- 必含指标链：会员数、年飞行次数均值、年消费总额(元)、高价值分层占比、里程余额均值
- 必含异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队
- 必含 Skill：rfm-segmentation、lifecycle-action、privacy-boundary

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得输出歧视性或不可解释规则」。
- 交付物文件：`outputs/product_case_library/case_30_airline_member_rfm_问题定义.md`、`…_方案验收.md`。


**指定实操融合**

- RP11：AI-Excel 数据分析转产品判断
  - 产出：指标分析表, 产品改进建议
  - 验收：RFM、趋势或异常检测结果必须转化为明确产品判断，不能停留在图表描述。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/30`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 31：漏斗分析｜广告投放转化复盘

> **本案例演示/验证**：原理 3.2、4.3｜**采用设计** `amber-funnel`（见 design/amber-funnel.md）

> **在数字化系统中的位置**：能力智能层 · 洞察环节｜**理论→实操**：把原理 3.2、4.3 落成可运行操作：判断漏斗断点、回款风险和区域销售动作优先级。（数字化底座本身）

> 🎚 **难度** 进阶｜🎯 **一句话** 广告投放转化复盘：判断漏斗断点、回款风险和区域销售动作优先级
>
> 💡 **洞见**：别只做「sales_funnel_screen」页面——把「渠道数」等指标 → 异常 → 责任 → 行动串成闭环，落在 洞察 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得只按点击率做预算决策」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

广告投放场景中，投放产品经理围绕广告转化漏斗做日常判断。投放产品经理需要围绕“广告转化漏斗”完成“判断预算加码、缩减或观察”，并把结果转成可验证的产品交付物。核心不是把页面做出来，而是把指标、异常、责任和行动连成闭环。

**现状问题**

- 决策依赖的关键指标：渠道数、总曝光、总点击、总转化、平均CPA。
- 现场常见异常：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 只做通用页面无法支撑「判断漏斗断点、回款风险和区域销售动作优先级。」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `funnel-analysis` 与 `budget-decision` 完成分析，产出 `漏斗与预算建议`，用 `metric-definition` 验收。

### 任务目标与数据

- 行业：广告投放
- 真实业务场景：广告投放转化复盘
- 岗位：投放产品经理
- 数据或资料：`dataset/reference_data_analysis/18-ad_performance.csv`（6 行，异常 6）
- 公开参考：https://carbondesignsystem.com/
- 行业字段：渠道、曝光、点击、注册、订单、预算
- 指标链（真实值）：渠道数 6，总曝光 1657329，总点击 73821，总转化 3869，平均CPA 39.1
- 决策动作：判断漏斗断点、回款风险和区域销售动作优先级。
- 风险边界：不得只按点击率做预算决策
- UI 原型：`ui_31_ad_conversion_funnel`（sales_funnel_screen）
- 采用设计：amber-funnel
- SaaS 组件：销售漏斗、目标进度、客户跟进、回款风险、区域作战、销售排行

### Prompt 实操

**Prompt 1：广告投放转化复盘 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「广告投放转化复盘」的产品问题定义，并产出可运行原型：
- 场景与岗位：投放产品经理 面向「广告投放转化复盘」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/18-ad_performance.csv`，只使用数据/资料中存在的字段（渠道、曝光、点击、注册、订单、预算）。
- 指标链：渠道数、总曝光、总点击、总转化、平均CPA（当前真实值：渠道数=6，总曝光=1657329，总点击=73821，总转化=3869，平均CPA=39.1）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：funnel-analysis、budget-decision，并用 metric-definition 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/31`，按 `ui_31_ad_conversion_funnel`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：漏斗与预算建议，保存为 `outputs/product_case_library/case_31_ad_conversion_funnel_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://carbondesignsystem.com/）；不得越过「不得只按点击率做预算决策」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：广告投放转化复盘 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「广告投放转化复盘」的方案验收，并产出可运行原型：
- 场景与岗位：投放产品经理 面向「广告投放转化复盘」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/reference_data_analysis/18-ad_performance.csv`，只使用数据/资料中存在的字段（渠道、曝光、点击、注册、订单、预算）。
- 指标链：渠道数、总曝光、总点击、总转化、平均CPA（当前真实值：渠道数=6，总曝光=1657329，总点击=73821，总转化=3869，平均CPA=39.1）。
- 异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队。
- 使用 Skill：funnel-analysis、budget-decision，并用 metric-definition 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/31`，按 `ui_31_ad_conversion_funnel`（sales_funnel_screen）与设计 `amber-funnel` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：漏斗与预算建议，保存为 `outputs/product_case_library/case_31_ad_conversion_funnel_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://carbondesignsystem.com/）；不得越过「不得只按点击率做预算决策」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![广告投放转化复盘 · 信息图](outputs/product_case_library/svg/case_31_ad_conversion_funnel.svg)

![广告投放转化复盘 · 可运行大屏原型截图](assets/screenshots/premium_case_31_ad_conversion_funnel_desktop.png)

- 图形类型：ad_conversion_funnel（设计 amber-funnel）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_31_ad_conversion_funnel` + 设计 `amber-funnel`，不得复用通用表格占位；可运行原型见 `#/case/31`。

### 交付物与验收

- 交付物：漏斗与预算建议
- 必含字段：渠道、曝光、点击、注册、订单、预算
- 必含指标链：渠道数、总曝光、总点击、总转化、平均CPA
- 必含异常状态：转化率下滑、回款逾期、客户跟进超时、区域掉队
- 必含 Skill：funnel-analysis、budget-decision、metric-definition

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得只按点击率做预算决策」。
- 交付物文件：`outputs/product_case_library/case_31_ad_conversion_funnel_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/31`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 41：综合闭环｜零售经营产品方案

> **本案例演示/验证**：原理 2.7、3.1、4.1｜**采用设计** `graphite-hud`（见 design/graphite-hud.md）

> **在数字化系统中的位置**：业务应用层 · 增长环节｜**理论→实操**：把原理 2.7、3.1、4.1 落成可运行操作：从数据端到端得出零售经营改进方案并落成可验收交付物（底座依赖 44/45/46/47）

> 🎚 **难度** 高阶｜🎯 **一句话** 零售经营产品方案：从数据端到端得出零售经营改进方案并落成可验收交付物
>
> 💡 **洞见**：别只做「executive_dashboard」页面——把「营收(元)」等指标 → 异常 → 责任 → 行动串成闭环，落在 增长 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得脱离数据编造结论」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

零售经营负责人要把经营数据端到端转成一份可验收的产品方案：从异常识别到改进动作到责任闭环，串起理念、架构与验收。

**现状问题**

- 决策依赖的关键指标：营收(元)、毛利率均值、品类数、异常订单率、平均处理时限(h)。
- 现场常见异常：目标未达成、滞销、毛利异常、责任未闭环。
- 只做通用页面无法支撑「从数据端到端得出零售经营改进方案并落成可验收交付物」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `capstone-product-flow` 与 `evidence-pack` 完成分析，产出 `零售经营产品方案`，用 `traceability-check` 验收。

### 任务目标与数据

- 行业：零售经营
- 真实业务场景：零售经营产品方案
- 岗位：经营负责人
- 数据或资料：`dataset/order_data.csv`（1200 行，异常 225）
- 公开参考：https://archive.ics.uci.edu/dataset/352/online+retail
- 行业字段：商品、门店、销量、毛利、库存、复购、责任人
- 指标链（真实值）：营收(元) 16630819，毛利率均值 33.03%，品类数 6，异常订单率 18.8%，平均处理时限(h) 7.12
- 决策动作：从数据端到端得出零售经营改进方案并落成可验收交付物
- 风险边界：不得脱离数据编造结论
- UI 原型：`ui_41_retail_capstone_board`（executive_dashboard）
- 采用设计：graphite-hud
- SaaS 组件：目标达成、收入趋势、业务结构、区域排行、异常告警、行动队列

### Prompt 实操

**Prompt 1：零售经营产品方案 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「零售经营产品方案」的产品问题定义，并产出可运行原型：
- 场景与岗位：经营负责人 面向「零售经营产品方案」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（商品、门店、销量、毛利、库存、复购、责任人）。
- 指标链：营收(元)、毛利率均值、品类数、异常订单率、平均处理时限(h)（当前真实值：营收(元)=16630819，毛利率均值=33.03%，品类数=6，异常订单率=18.8%，平均处理时限(h)=7.12）。
- 异常状态：目标未达成、滞销、毛利异常、责任未闭环。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/41`，按 `ui_41_retail_capstone_board`（executive_dashboard）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：零售经营产品方案，保存为 `outputs/product_case_library/case_41_retail_capstone_board_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得脱离数据编造结论」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：零售经营产品方案 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「零售经营产品方案」的方案验收，并产出可运行原型：
- 场景与岗位：经营负责人 面向「零售经营产品方案」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（商品、门店、销量、毛利、库存、复购、责任人）。
- 指标链：营收(元)、毛利率均值、品类数、异常订单率、平均处理时限(h)（当前真实值：营收(元)=16630819，毛利率均值=33.03%，品类数=6，异常订单率=18.8%，平均处理时限(h)=7.12）。
- 异常状态：目标未达成、滞销、毛利异常、责任未闭环。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/41`，按 `ui_41_retail_capstone_board`（executive_dashboard）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：零售经营产品方案，保存为 `outputs/product_case_library/case_41_retail_capstone_board_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://archive.ics.uci.edu/dataset/352/online+retail）；不得越过「不得脱离数据编造结论」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![零售经营产品方案 · 信息图](outputs/product_case_library/svg/case_41_retail_capstone_board.svg)

![零售经营产品方案 · 可运行大屏原型截图](assets/screenshots/premium_case_41_retail_capstone_board_desktop.png)

- 图形类型：retail_capstone_board（设计 graphite-hud）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_41_retail_capstone_board` + 设计 `graphite-hud`，不得复用通用表格占位；可运行原型见 `#/case/41`。

### 交付物与验收

- 交付物：零售经营产品方案
- 必含字段：商品、门店、销量、毛利、库存、复购、责任人
- 必含指标链：营收(元)、毛利率均值、品类数、异常订单率、平均处理时限(h)
- 必含异常状态：目标未达成、滞销、毛利异常、责任未闭环
- 必含 Skill：capstone-product-flow、evidence-pack、traceability-check

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得脱离数据编造结论」。
- 交付物文件：`outputs/product_case_library/case_41_retail_capstone_board_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/41`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 44：AI协作｜知识库语义检索(RAG)

> **本案例演示/验证**：原理 1.3、3.3｜**采用设计** `emerald-flow`（见 design/emerald-flow.md）

> **在数字化系统中的位置**：底座平台层 · 治理环节｜**理论→实操**：把原理 1.3、3.3 落成可运行操作：用真实向量检索为问答/推荐提供高相关片段，替代全量塞入（数字化底座本身）

> 🎚 **难度** 高阶｜🎯 **一句话** 产品知识库语义检索：用真实向量检索为问答/推荐提供高相关片段，替代全量塞入
>
> 💡 **洞见**：别只做「rag_search」页面——把「语料规模」等指标 → 异常 → 责任 → 行动串成闭环，落在 治理 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「不得把低相似片段当作事实回答」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

AI 产品经理要把上千页产品知识做成语义检索：不是全量塞入大模型，而是用向量库只召回与问题高相关的片段（对应 §1.3 上下文/RAG）。前端调 /api/search 实时展示命中与相似度。

**现状问题**

- 决策依赖的关键指标：语料规模、命中率、Top1 相似度、检索延迟。
- 现场常见异常：低相似、无命中、越权语料。
- 只做通用页面无法支撑「用真实向量检索为问答/推荐提供高相关片段，替代全量塞入」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `capstone-product-flow` 与 `evidence-pack` 完成分析，产出 `RAG 检索方案与验收`，用 `traceability-check` 验收。

### 任务目标与数据

- 行业：AI 应用
- 真实业务场景：产品知识库语义检索
- 岗位：AI 产品经理
- 数据或资料：`skills/external/pm-skills-deanpeters/README.md`（46 行，异常 0）
- 公开参考：本地：skills/external/pm-skills-deanpeters（RAG 语料）｜pgvector github.com/pgvector/pgvector
- 行业字段：查询、命中文档、相似度、片段
- 指标链（真实值）：语料规模 22，命中率 33%，Top1 相似度 44，检索延迟 55
- 决策动作：用真实向量检索为问答/推荐提供高相关片段，替代全量塞入
- 风险边界：不得把低相似片段当作事实回答
- UI 原型：`ui_44_rag_retrieval`（rag_search）
- 采用设计：emerald-flow
- SaaS 组件：查询框、命中列表、相似度条、片段预览、语料统计

### Prompt 实操

**Prompt 1：产品知识库语义检索 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「产品知识库语义检索」的产品问题定义，并产出可运行原型：
- 场景与岗位：AI 产品经理 面向「产品知识库语义检索」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `skills/external/pm-skills-deanpeters/README.md`，只使用数据/资料中存在的字段（查询、命中文档、相似度、片段）。
- 指标链：语料规模、命中率、Top1 相似度、检索延迟（当前真实值：语料规模=22，命中率=33%，Top1 相似度=44，检索延迟=55）。
- 异常状态：低相似、无命中、越权语料。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/44`，按 `ui_44_rag_retrieval`（rag_search）与设计 `emerald-flow` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：RAG 检索方案与验收，保存为 `outputs/product_case_library/case_44_rag_knowledge_retrieval_问题定义.md`。
- 验收条件：结论回到数据或公开参考（本地：skills/external/pm-skills-deanpeters（RAG 语料）｜pgvector github.com/pgvector/pgvector）；不得越过「不得把低相似片段当作事实回答」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：产品知识库语义检索 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「产品知识库语义检索」的方案验收，并产出可运行原型：
- 场景与岗位：AI 产品经理 面向「产品知识库语义检索」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `skills/external/pm-skills-deanpeters/README.md`，只使用数据/资料中存在的字段（查询、命中文档、相似度、片段）。
- 指标链：语料规模、命中率、Top1 相似度、检索延迟（当前真实值：语料规模=22，命中率=33%，Top1 相似度=44，检索延迟=55）。
- 异常状态：低相似、无命中、越权语料。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/44`，按 `ui_44_rag_retrieval`（rag_search）与设计 `emerald-flow` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：RAG 检索方案与验收，保存为 `outputs/product_case_library/case_44_rag_knowledge_retrieval_方案验收.md`。
- 验收条件：结论回到数据或公开参考（本地：skills/external/pm-skills-deanpeters（RAG 语料）｜pgvector github.com/pgvector/pgvector）；不得越过「不得把低相似片段当作事实回答」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![产品知识库语义检索 · 信息图](outputs/product_case_library/svg/case_44_rag_knowledge_retrieval.svg)

![产品知识库语义检索 · 可运行大屏原型截图](assets/screenshots/premium_case_44_rag_knowledge_retrieval_desktop.png)

- 图形类型：rag_knowledge_retrieval（设计 emerald-flow）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_44_rag_retrieval` + 设计 `emerald-flow`，不得复用通用表格占位；可运行原型见 `#/case/44`。

### 交付物与验收

- 交付物：RAG 检索方案与验收
- 必含字段：查询、命中文档、相似度、片段
- 必含指标链：语料规模、命中率、Top1 相似度、检索延迟
- 必含异常状态：低相似、无命中、越权语料
- 必含 Skill：capstone-product-flow、evidence-pack、traceability-check

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「不得把低相似片段当作事实回答」。
- 交付物文件：`outputs/product_case_library/case_44_rag_knowledge_retrieval_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/44`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 45：系统架构｜关系库查询(PostgreSQL)

> **本案例演示/验证**：原理 3.3、4.1｜**采用设计** `steel-queue`（见 design/steel-queue.md）

> **在数字化系统中的位置**：底座平台层 · 治理环节｜**理论→实操**：把原理 3.3、4.1 落成可运行操作：用真实 SQL（建表/索引/参数化聚合）支撑经营分析，讲清 PostgreSQL 架构（数字化底座本身）

> 🎚 **难度** 高阶｜🎯 **一句话** 经营数据关系库查询：用真实 SQL（建表/索引/参数化聚合）支撑经营分析，讲清 PostgreSQL 架构
>
> 💡 **洞见**：别只做「db_console」页面——把「表行数」等指标 → 异常 → 责任 → 行动串成闭环，落在 治理 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「本地 SQLite 演示、生产为 PostgreSQL；不得把演示数据当真账」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

数据产品经理要把经营 CSV 落成可查询的关系库：真实 CREATE TABLE + 索引 + 参数化聚合（前端调 /api/db/query），并讲清 PostgreSQL/pgvector 的生产架构与本地 SQLite 演示的差异。

**现状问题**

- 决策依赖的关键指标：表行数、区域数、品类数、总销售额(元)。
- 现场常见异常：慢查询、全表扫描、空结果。
- 只做通用页面无法支撑「用真实 SQL（建表/索引/参数化聚合）支撑经营分析，讲清 PostgreSQL 架构」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `capstone-product-flow` 与 `evidence-pack` 完成分析，产出 `关系库查询与 PG 架构说明`，用 `traceability-check` 验收。

### 任务目标与数据

- 行业：数据工程
- 真实业务场景：经营数据关系库查询
- 岗位：数据产品经理
- 数据或资料：`dataset/order_data.csv`（1200 行，异常 225）
- 公开参考：https://www.postgresql.org/docs/current/ ｜ pgvector github.com/pgvector/pgvector
- 行业字段：SKU、品类、区域、金额、毛利率
- 指标链（真实值）：表行数 1200，区域数 6，品类数 6，总销售额(元) 16630819
- 决策动作：用真实 SQL（建表/索引/参数化聚合）支撑经营分析，讲清 PostgreSQL 架构
- 风险边界：本地 SQLite 演示、生产为 PostgreSQL；不得把演示数据当真账
- UI 原型：`ui_45_pg_relational`（db_console）
- 采用设计：steel-queue
- SaaS 组件：表结构、SQL 查询、结果表、区域聚合、执行说明

### Prompt 实操

**Prompt 1：经营数据关系库查询 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「经营数据关系库查询」的产品问题定义，并产出可运行原型：
- 场景与岗位：数据产品经理 面向「经营数据关系库查询」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（SKU、品类、区域、金额、毛利率）。
- 指标链：表行数、区域数、品类数、总销售额(元)（当前真实值：表行数=1200，区域数=6，品类数=6，总销售额(元)=16630819）。
- 异常状态：慢查询、全表扫描、空结果。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/45`，按 `ui_45_pg_relational`（db_console）与设计 `steel-queue` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：关系库查询与 PG 架构说明，保存为 `outputs/product_case_library/case_45_postgres_relational_arch_问题定义.md`。
- 验收条件：结论回到数据或公开参考（https://www.postgresql.org/docs/current/ ｜ pgvector github.com/pgvector/pgvector）；不得越过「本地 SQLite 演示、生产为 PostgreSQL；不得把演示数据当真账」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：经营数据关系库查询 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「经营数据关系库查询」的方案验收，并产出可运行原型：
- 场景与岗位：数据产品经理 面向「经营数据关系库查询」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（SKU、品类、区域、金额、毛利率）。
- 指标链：表行数、区域数、品类数、总销售额(元)（当前真实值：表行数=1200，区域数=6，品类数=6，总销售额(元)=16630819）。
- 异常状态：慢查询、全表扫描、空结果。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/45`，按 `ui_45_pg_relational`（db_console）与设计 `steel-queue` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：关系库查询与 PG 架构说明，保存为 `outputs/product_case_library/case_45_postgres_relational_arch_方案验收.md`。
- 验收条件：结论回到数据或公开参考（https://www.postgresql.org/docs/current/ ｜ pgvector github.com/pgvector/pgvector）；不得越过「本地 SQLite 演示、生产为 PostgreSQL；不得把演示数据当真账」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![经营数据关系库查询 · 信息图](outputs/product_case_library/svg/case_45_postgres_relational_arch.svg)

![经营数据关系库查询 · 可运行大屏原型截图](assets/screenshots/premium_case_45_postgres_relational_arch_desktop.png)

- 图形类型：postgres_relational_arch（设计 steel-queue）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_45_pg_relational` + 设计 `steel-queue`，不得复用通用表格占位；可运行原型见 `#/case/45`。

### 交付物与验收

- 交付物：关系库查询与 PG 架构说明
- 必含字段：SKU、品类、区域、金额、毛利率
- 必含指标链：表行数、区域数、品类数、总销售额(元)
- 必含异常状态：慢查询、全表扫描、空结果
- 必含 Skill：capstone-product-flow、evidence-pack、traceability-check

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「本地 SQLite 演示、生产为 PostgreSQL；不得把演示数据当真账」。
- 交付物文件：`outputs/product_case_library/case_45_postgres_relational_arch_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/45`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 46：系统架构｜子系统分解与接口契约

> **本案例演示/验证**：原理 3.1、3.3｜**采用设计** `cyan-matrix`（见 design/cyan-matrix.md）

> **在数字化系统中的位置**：底座平台层 · 治理环节｜**理论→实操**：把原理 3.1、3.3 落成可运行操作：把 §3 架构流程落到本仓库真运行后端：分层/边界/契约在代码里可查（数字化底座本身）

> 🎚 **难度** 高阶｜🎯 **一句话** 后端子系统分解与契约：把 §3 架构流程落到本仓库真运行后端：分层/边界/契约在代码里可查
>
> 💡 **洞见**：别只做「arch_board」页面——把「子系统数」等指标 → 异常 → 责任 → 行动串成闭环，落在 治理 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「架构决策须可追溯（ADR），不得口头拍板」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

架构型产品经理以本教程后端自身为例演示系统架构设计：routes/services/data/db/vector 分层与模块边界、一次真实接口契约调用（/api/health、错误信封），把 §3 方法论落到可运行代码（dogfood）。

**现状问题**

- 决策依赖的关键指标：子系统数、接口数、契约覆盖、健康检查。
- 现场常见异常：职责越界、契约缺失、循环依赖。
- 只做通用页面无法支撑「把 §3 架构流程落到本仓库真运行后端：分层/边界/契约在代码里可查」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `capstone-product-flow` 与 `evidence-pack` 完成分析，产出 `子系统分解图与接口契约`，用 `traceability-check` 验收。

### 任务目标与数据

- 行业：系统架构
- 真实业务场景：后端子系统分解与契约
- 岗位：架构型产品经理
- 数据或资料：`rules/backend.md`（46 行，异常 0）
- 公开参考：Software Engineering at Google https://abseil.io/resources/swe-book/ ｜ 12factor.net
- 行业字段：子系统、职责、接口、契约
- 指标链（真实值）：子系统数 22，接口数 33，契约覆盖 44，健康检查 55
- 决策动作：把 §3 架构流程落到本仓库真运行后端：分层/边界/契约在代码里可查
- 风险边界：架构决策须可追溯（ADR），不得口头拍板
- UI 原型：`ui_46_arch_contract`（arch_board）
- 采用设计：cyan-matrix
- SaaS 组件：子系统图、分层边界、接口契约、健康检查、ADR 记录

### Prompt 实操

**Prompt 1：后端子系统分解与契约 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「后端子系统分解与契约」的产品问题定义，并产出可运行原型：
- 场景与岗位：架构型产品经理 面向「后端子系统分解与契约」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `rules/backend.md`，只使用数据/资料中存在的字段（子系统、职责、接口、契约）。
- 指标链：子系统数、接口数、契约覆盖、健康检查（当前真实值：子系统数=22，接口数=33，契约覆盖=44，健康检查=55）。
- 异常状态：职责越界、契约缺失、循环依赖。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/46`，按 `ui_46_arch_contract`（arch_board）与设计 `cyan-matrix` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：子系统分解图与接口契约，保存为 `outputs/product_case_library/case_46_system_arch_flow_问题定义.md`。
- 验收条件：结论回到数据或公开参考（Software Engineering at Google https://abseil.io/resources/swe-book/ ｜ 12factor.net）；不得越过「架构决策须可追溯（ADR），不得口头拍板」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：后端子系统分解与契约 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「后端子系统分解与契约」的方案验收，并产出可运行原型：
- 场景与岗位：架构型产品经理 面向「后端子系统分解与契约」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `rules/backend.md`，只使用数据/资料中存在的字段（子系统、职责、接口、契约）。
- 指标链：子系统数、接口数、契约覆盖、健康检查（当前真实值：子系统数=22，接口数=33，契约覆盖=44，健康检查=55）。
- 异常状态：职责越界、契约缺失、循环依赖。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/46`，按 `ui_46_arch_contract`（arch_board）与设计 `cyan-matrix` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：子系统分解图与接口契约，保存为 `outputs/product_case_library/case_46_system_arch_flow_方案验收.md`。
- 验收条件：结论回到数据或公开参考（Software Engineering at Google https://abseil.io/resources/swe-book/ ｜ 12factor.net）；不得越过「架构决策须可追溯（ADR），不得口头拍板」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![后端子系统分解与契约 · 信息图](outputs/product_case_library/svg/case_46_system_arch_flow.svg)

![后端子系统分解与契约 · 可运行大屏原型截图](assets/screenshots/premium_case_46_system_arch_flow_desktop.png)

- 图形类型：system_arch_flow（设计 cyan-matrix）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_46_arch_contract` + 设计 `cyan-matrix`，不得复用通用表格占位；可运行原型见 `#/case/46`。

### 交付物与验收

- 交付物：子系统分解图与接口契约
- 必含字段：子系统、职责、接口、契约
- 必含指标链：子系统数、接口数、契约覆盖、健康检查
- 必含异常状态：职责越界、契约缺失、循环依赖
- 必含 Skill：capstone-product-flow、evidence-pack、traceability-check

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「架构决策须可追溯（ADR），不得口头拍板」。
- 交付物文件：`outputs/product_case_library/case_46_system_arch_flow_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/46`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。


---

## 实操 47：数据指标｜三维经营散点(three.js)

> **本案例演示/验证**：原理 3.3、5.1｜**采用设计** `graphite-hud`（见 design/graphite-hud.md）

> **在数字化系统中的位置**：底座平台层 · 洞察环节｜**理论→实操**：把原理 3.3、5.1 落成可运行操作：用 three.js/R3F 把经营数据渲染成可交互三维散点，暴露多维离群（数字化底座本身）

> 🎚 **难度** 进阶｜🎯 **一句话** 经营三维散点可视化：用 three.js/R3F 把经营数据渲染成可交互三维散点，暴露多维离群
>
> 💡 **洞见**：别只做「scene_3d」页面——把「数据点数」等指标 → 异常 → 责任 → 行动串成闭环，落在 洞察 环节，才是这个案例的价值。
>
> ⚠ **常见坑**：常见坑：越过「三维仅作探索，不替代统计结论」，或用泛化指标硬套——本案例指标已按数据集真实列（metricSpec）真算，别再编造。

### 项目场景故事

可视化产品经理用 react-three-fiber 把经营数据（单价×数量×金额，色=品类）渲染成真实三维散点（后端 /api/points3d 供点），在大屏上交互探索多维离群。

**现状问题**

- 决策依赖的关键指标：数据点数、品类数、最大金额(元)、客单价均值(元)。
- 现场常见异常：离群点、空品类。
- 只做通用页面无法支撑「用 three.js/R3F 把经营数据渲染成可交互三维散点，暴露多维离群」。

**本次任务**

- 明确岗位、指标链、异常状态与决策动作。
- 使用 `capstone-product-flow` 与 `evidence-pack` 完成分析，产出 `三维散点可视化方案`，用 `traceability-check` 验收。

### 任务目标与数据

- 行业：数据可视化
- 真实业务场景：经营三维散点可视化
- 岗位：可视化产品经理
- 数据或资料：`dataset/order_data.csv`（1200 行，异常 225）
- 公开参考：React Three Fiber https://github.com/pmndrs/react-three-fiber
- 行业字段：品类、单价、数量、金额
- 指标链（真实值）：数据点数 1200，品类数 6，最大金额(元) 51121，客单价均值(元) 451.98
- 决策动作：用 three.js/R3F 把经营数据渲染成可交互三维散点，暴露多维离群
- 风险边界：三维仅作探索，不替代统计结论
- UI 原型：`ui_47_three_scatter`（scene_3d）
- 采用设计：graphite-hud
- SaaS 组件：三维散点、坐标轴、品类图例、离群标记、轨道控制

### Prompt 实操

**Prompt 1：经营三维散点可视化 - 问题定义**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「经营三维散点可视化」的产品问题定义，并产出可运行原型：
- 场景与岗位：可视化产品经理 面向「经营三维散点可视化」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（品类、单价、数量、金额）。
- 指标链：数据点数、品类数、最大金额(元)、客单价均值(元)（当前真实值：数据点数=1200，品类数=6，最大金额(元)=51121，客单价均值(元)=451.98）。
- 异常状态：离群点、空品类。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/47`，按 `ui_47_three_scatter`（scene_3d）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：三维散点可视化方案，保存为 `outputs/product_case_library/case_47_three_d_scatter_问题定义.md`。
- 验收条件：结论回到数据或公开参考（React Three Fiber https://github.com/pmndrs/react-three-fiber）；不得越过「三维仅作探索，不替代统计结论」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

**Prompt 2：经营三维散点可视化 - 方案验收**

```text
请以产品经理身份，用 AI 编程工具（如 Trae、CodeBuddy 等任一 Agent 工具）完成「经营三维散点可视化」的方案验收，并产出可运行原型：
- 场景与岗位：可视化产品经理 面向「经营三维散点可视化」，把判断转成可验证的产品交付物；核心是把指标→异常→责任→行动连成闭环。
- 数据：读取 `dataset/order_data.csv`，只使用数据/资料中存在的字段（品类、单价、数量、金额）。
- 指标链：数据点数、品类数、最大金额(元)、客单价均值(元)（当前真实值：数据点数=1200，品类数=6，最大金额(元)=51121，客单价均值(元)=451.98）。
- 异常状态：离群点、空品类。
- 使用 Skill：capstone-product-flow、evidence-pack，并用 traceability-check 做验收（结构化 Skill 见 skills/pm_skills.md）。
- 原型（技术契约，遵 rules/ 约束：DRY、单文件<800行、TS 类型、中文注释）：在 `code/web`（Vite+React+TS）路由 `#/case/47`，按 `ui_47_three_scatter`（scene_3d）与设计 `graphite-hud` 渲染；数据经 `build_case_data.mjs` 预计算，页面必须体现指标链、异常队列、责任对象与行动入口，不得复用通用表格占位。
- 输出：三维散点可视化方案，保存为 `outputs/product_case_library/case_47_three_d_scatter_方案验收.md`。
- 验收条件：结论回到数据或公开参考（React Three Fiber https://github.com/pmndrs/react-three-fiber）；不得越过「三维仅作探索，不替代统计结论」；`node code/tools/verify_course_package.mjs` 必须 ALL GREEN。
```

### 图形/原型/表单

![经营三维散点可视化 · 信息图](outputs/product_case_library/svg/case_47_three_d_scatter.svg)

![经营三维散点可视化 · 可运行大屏原型截图](assets/screenshots/premium_case_47_three_d_scatter_desktop.png)

- 图形类型：three_d_scatter（设计 graphite-hud）
- 看图顺序：先看指标链，再看异常队列和责任对象，最后看行动入口与验收边界。
- UI 差异：本案例采用 `ui_47_three_scatter` + 设计 `graphite-hud`，不得复用通用表格占位；可运行原型见 `#/case/47`。

### 交付物与验收

- 交付物：三维散点可视化方案
- 必含字段：品类、单价、数量、金额
- 必含指标链：数据点数、品类数、最大金额(元)、客单价均值(元)
- 必含异常状态：离群点、空品类
- 必含 Skill：capstone-product-flow、evidence-pack、traceability-check

- 合格标准：业务场景具体、指标链完整、异常状态可追踪、行动入口明确、验收条件可执行。
- 不合格标准：使用泛化产品名称、缺少行业指标、只描述页面不说明业务取舍、越过「三维仅作探索，不替代统计结论」。
- 交付物文件：`outputs/product_case_library/case_47_three_d_scatter_问题定义.md`、`…_方案验收.md`。


> 运行本案例：`bash code/run.sh` 起服务后访问 `#/case/47`（真后端实时数据）。跑通/纠错/约束的统一说明见「使用入口」，不再逐案例赘述。

