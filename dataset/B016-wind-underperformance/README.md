# 案例 16 · 风机出力下偏核查

`case.csv` 有 938 条风机日记录，来自 SDWPF 的七天切片；`turbine-locations.csv` 保存 134 台风机的公开位置。

`underperformance_share` 是把原始点位的下偏标记按天求平均得到的字段。本地结果目前只有 0 和 1，因此页面称它为“日级下偏标记”，不称为故障概率或连续占比。

数据没有预期功率、同群基线、限电指令、告警和维修结果。页面只提交现场核查或请求补数据。
