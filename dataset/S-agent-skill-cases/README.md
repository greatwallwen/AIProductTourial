# S01—S08 Agent + Skills 课程摘要

`case.csv` 有 32 条记录。每个案例各用“观察、选择、动作、检查”四行概括当前本地工件，便于教程和验证器保持同一口径。

这些行不是在线 Agent 的原始对话或隐藏推理，也不证明外部 Provider 调用或业务成效。S04 的四行会直接进入机会图生成；其余行是课程索引，具体数字和文件以 `runtime/agent-skills/S01` 至 `S08` 的正式工件与回执为准。
