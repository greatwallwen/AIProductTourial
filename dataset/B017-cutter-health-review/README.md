# 案例 17 · 包装切刀健康复核

`case.csv` 有 519 个会话摘要，来自 inIT-OWL 工业部件劣化数据。健康偏差和复核等级是课程规则计算的排检线索，不是磨损率、剩余寿命或故障原因。

`waveform.csv` 只包含 `BD-0001` 到 `BD-0008` 的 256 点波形。页面只有在会话确实存在波形时才开放波形查看；默认样例使用 `BD-0003`。

数据没有准确时间轴、刀片更换史、产品质量、排产窗口和停机代价。页面只确认排检候选。
