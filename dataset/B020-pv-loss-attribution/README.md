# 案例 20 · 光伏少发站端核查

`case.csv` 有 5,327 条电站日记录，来自中国国家电网新能源发电预测竞赛公开数据，覆盖 8 个匿名站点。日级日期、记录数、辐照度、气温和出力由上游记录聚合；缺失日保留为空，不补成 0。

`mean_efficiency_ratio` 是上游 `eff_ratio` 的日均值，页面称为“归一化出力比”，不是组件物理转换效率。温度降额和疑似限电占比来自上游派生字段的日均值，只能作为核查线索；其计算公式不在本地快照中，必须结合调度记录、逆变器告警和现场工单核实。

页面只提交站端核查并确认核查方向，不自动改变电站控制设定。
