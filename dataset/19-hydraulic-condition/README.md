# 案例 19 · 液压部件检查排序

`case.csv` 有 2,205 次测量循环，来自 UCI 液压系统状态监测。主键是 `cycle_id`；数据没有液压站编号、真实时间戳或部件资产号。

表中保留主压力、回油压力、系统压力、电机功率、主流量、油箱温度和系统振动 7 项循环均值，以及冷却器、阀、泵和蓄能器 4 类状态。连续量由公开原始测点按循环归一，中文状态和严重度由交付脚本映射；页面只帮助人工排出检查顺序，不能把状态标签写成维修结论，也不会自动创建工单。
