{
  "name": "pm-kb-server",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node --experimental-sqlite app.ts",
    "test": "node --experimental-sqlite --test tests/*.test.ts"
  },
  "dependencies": {
    "fastify": "^5.10.0",
    "@fastify/static": "^9.3.0"
  }
}
